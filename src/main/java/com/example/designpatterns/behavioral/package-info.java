/**
 * Behavioral design pattern demos (Strategy, Observer, Command, etc.).
 * Empty for now - add new patterns here as new classes/subpackages rather
 * than as new Gradle projects or top-level folders.
 */
package com.example.designpatterns.behavioral;
